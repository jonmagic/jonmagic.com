{% extends "layout.njk" %}

{% block content %}
  <article class="post">
    <h1>{{ title }}</h1>
    <p class="post-date">{{ date | date("MMMM d, yyyy") }}</p>
    {{ content | safe }}
  </article>
{% endblock %}
